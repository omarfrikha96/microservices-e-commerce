spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost:8098/realms/ms"
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: gateway-service

